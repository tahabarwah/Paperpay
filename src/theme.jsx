import { createTheme } from "@mui/material/styles";

const theme = createTheme({
  typography: {
    fontFamily: "Outfit", // Your custom font family
  },
  palette: {
    primary: {
      main: "#8288AD", // Your custom primary color
    },
    // Add other customizations as needed
  },
});

export default theme;
